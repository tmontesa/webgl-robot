//
// objects/mesh.js
// =========
// A struct that contains the needed information that makes up a mesh.
//

function Mesh(vertices, indices) {
    this.vertices = vertices;
    this.indices = indices;
}